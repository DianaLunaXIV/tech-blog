{{#each posts as |post|}}
<div class="post-preview">
    <div class="post-meta">
        <h3><a href="/post/{{post.id}}">{{post.title}}</a></h3>
        <h4>Posted by: {{post.user.name}}</h4>
        <p>{{post.body}}</p>
    </div>
</div>
{{/each}}